It's now possible to name code blocks by passing them a `:slot:` option to override the default `content` slot.
Slots can be included in other code blocks using the `{{ insert(slots.name) }}` template function.
